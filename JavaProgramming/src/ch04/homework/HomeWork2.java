package ch04.homework;

public class HomeWork2 {

	public static void main(String[] args) {

		// int형 변수 x를 선언하고 최초값 1을 대입 후 조건이 맞으면 실행 후 1씩 증가함
		for (int x = 1; x <= 10; x++) {

			int y = 1; // x가 1~10 일때 y에 1 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
			y = 2; // x가 1~10 일때 y에 2 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
			y = 3; // x가 1~10 일때 y에 3 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
			y = 4; // x가 1~10 일때 y에 4 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
			y = 5; // x가 1~10 일때 y에 5 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
			y = 6; // x가 1~10 일때 y에 6 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
			y = 7; // x가 1~10 일때 y에 7 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
			y = 8; // x가 1~10 일때 y에 8 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
			y = 9; // x가 1~10 일때 y에 9 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
			y = 10; // x가 1~10 일때 y에 10 대입
			if (4 * x + 5 * y == 60) { // 조건문이 맞는지 검증
				// 방정식이 참이면 결과 출력
				System.out.println("(" + x + ", " + y + ")");
			}
		}
	}
}
